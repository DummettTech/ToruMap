{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "serve": "npm run build && vite preview"
  },
  "devDependencies": {
    "cpx": "latest",
    "vite": "latest"
  },
  "dependencies": {
    "ol": "^10.0.0"
  }
}
